"use client";
import ManualEntryData from "@/app/_components/OLD-DELETE/ManualEntryData/ManualEntryData.view";

export function ManualEntryView() {
  // const params = useParams<{ siteId: string }>();
  // const searchParams = useSearchParams();
  // const sitesIds = searchParams.get("sites-ids");

  return (
    <>
      <ManualEntryData />
    </>
  );
}
